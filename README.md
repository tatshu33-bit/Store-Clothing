# 👕 ClothesShop - Інтернет-магазин одягу

Повнофункціональний веб-додаток інтернет-магазину одягу, розроблений з використанням Flask та SQLite. Підтримує управління товарами, замовленнями, відгуками та адміністративну панель.

## 🌟 Основні можливості

### Для користувачів:
- 🛍️ **Каталог товарів** - перегляд доступних товарів з фільтрацією
- 🛒 **Кошик покупок** - додавання/видалення товарів, оформлення замовлень
- ⭐ **Система відгуків** - залишення відгуків з рейтингом (1-5 зірок)
- 📦 **Оформлення замовлень** - створення замовлень з автоматичним збереженням в БД
- 📱 **Адаптивний дизайн** - підтримка мобільних пристроїв (Bootstrap 5)

### Для адміністраторів:
- 📊 **Панель управління** - централізована адмін-панель
- 📦 **Управління товарами** - CRUD операції (створення, читання, оновлення, видалення)
- 📈 **Управління замовленнями** - перегляд, оновлення статусів (pending → processing → completed/canceled)
- 💬 **Управління відгуками** - перегляд та видалення відгуків користувачів
- 🔐 **Автентифікація** - захищений доступ до адмін-панелі

## 🗄️ Структура бази даних

### Таблиця `products` (Товари):
- `id` - унікальний ідентифікатор
- `title` - назва товару
- `description` - опис товару
- `price` - ціна (REAL)
- `stock` - кількість на складі (INTEGER)
- `category` - категорія товару
- `image_url` - посилання на зображення

### Таблиця `orders` (Замовлення):
- `id` - унікальний ідентифікатор
- `customer_name` - ім'я клієнта
- `customer_email` - email клієнта
- `status` - статус замовлення (pending/processing/completed/canceled)
- `total` - загальна сума (REAL)
- `created_at` - дата створення (TIMESTAMP)

### Таблиця `order_items` (Позиції замовлення):
- `id` - унікальний ідентифікатор
- `order_id` - зовнішній ключ до orders
- `product_id` - зовнішній ключ до products
- `quantity` - кількість (INTEGER)
- `price` - ціна на момент замовлення (REAL)

### Таблиця `feedback` (Відгуки):
- `id` - унікальний ідентифікатор
- `user_id` - ідентифікатор користувача (опційно)
- `name` - ім'я автора відгуку
- `email` - email автора
- `message` - текст відгуку
- `rating` - оцінка 1-5 (з CHECK constraint)
- `created_at` - дата створення (TIMESTAMP)

## 🚀 Швидкий старт

### Локальна розробка

1. **Клонувати репозиторій:**
```bash
git clone https://github.com/tatshu33-bit/Store-Clothing.git
cd Store-Clothing
```

2. **Встановити залежності:**
```bash
pip install -r requirements.txt
```

3. **Налаштувати змінні середовища (опційно):**
```bash
cp .env.example .env
# Відредагуйте .env файл з вашими налаштуваннями
```

4. **Запустити додаток:**
```bash
python app.py
```

5. **Відкрити в браузері:**
- Головна сторінка: http://127.0.0.1:5000
- Адмін-панель: http://127.0.0.1:5000/admin/login
- Пароль за замовчуванням: `adminpass`

### Розгортання з Docker

1. **Збудувати та запустити контейнер:**
```bash
docker-compose up --build
```

2. **Або використовувати Docker окремо:**
```bash
docker build -t clothesshop .
docker run -p 5000:5000 -e SECRET_KEY="your-secret-key" clothesshop
```

## 📋 Вимоги

- Python 3.8+
- Flask 3.1.2
- SQLite3 (вбудований)
- Додаткові залежності в `requirements.txt`

## 🏗️ Архітектура проєкту

```
Store-Clothing/
├── app.py                 # Головний файл додатку, конфігурація
├── models.py             # Моделі даних, робота з БД
├── routes/               # Маршрути (blueprints)
│   ├── __init__.py
│   ├── shop.py          # Магазин, кошик, оформлення
│   ├── feedback.py      # Відгуки користувачів
│   └── admin.py         # Адміністративна панель
├── templates/            # HTML шаблони (Jinja2)
│   ├── base.html        # Базовий шаблон
│   ├── index.html       # Головна сторінка
│   ├── shop.html        # Каталог товарів
│   ├── cart.html        # Кошик покупок
│   ├── checkout.html    # Оформлення замовлення
│   ├── feedback.html    # Сторінка відгуків
│   ├── admin_*.html     # Адмін-панель
│   ├── 404.html         # Помилка 404
│   └── 500.html         # Помилка 500
├── requirements.txt      # Python залежності
├── Dockerfile           # Docker конфігурація
├── docker-compose.yml   # Docker Compose файл
├── .env.example         # Приклад змінних середовища
├── .gitignore          # Git ignore правила
└── README.md           # Документація

```

## 🔐 Безпека

### Важливо для production:
1. **Змініть SECRET_KEY** - використовуйте випадковий ключ:
```python
import secrets
print(secrets.token_hex(32))
```

2. **Змініть ADMIN_PASSWORD** - встановіть надійний пароль

3. **Налаштуйте HTTPS** - використовуйте SSL сертифікати

4. **Обмежте DEBUG режим** - встановіть `FLASK_ENV=production`

## 🌐 Розгортання на хмарних платформах

### Render
1. Створіть новий Web Service
2. Підключіть GitHub репозиторій
3. Налаштуйте:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn app:app`
   - Додайте змінні середовища

### Heroku
```bash
heroku create your-app-name
heroku config:set SECRET_KEY="your-secret-key"
heroku config:set ADMIN_PASSWORD="your-password"
git push heroku main
```

### Railway
1. Імпортуйте проєкт з GitHub
2. Railway автоматично визначить Dockerfile
3. Додайте змінні середовища в Settings

## 📝 API Маршрути

### Публічні маршрути:
- `GET /` - Головна сторінка
- `GET /shop` - Каталог товарів
- `GET /product/<id>` - Деталі товару
- `GET /cart` - Перегляд кошика
- `POST /cart/add/<id>` - Додати товар до кошика
- `POST /cart/remove/<id>` - Видалити товар з кошика
- `GET /checkout` - Сторінка оформлення замовлення
- `POST /checkout` - Створити замовлення
- `GET /feedback` - Сторінка відгуків
- `POST /feedback` - Створити відгук

### Адміністративні маршрути:
- `GET /admin/login` - Сторінка входу адміністратора
- `POST /admin/login` - Автентифікація адміністратора
- `GET /admin/` - Панель управління (дашборд)
- `GET /admin/products` - Список товарів
- `GET /admin/products/add` - Форма додавання товару
- `POST /admin/products/add` - Створити товар
- `GET /admin/products/edit/<id>` - Форма редагування товару
- `POST /admin/products/edit/<id>` - Оновити товар
- `POST /admin/products/delete/<id>` - Видалити товар
- `GET /admin/orders` - Список замовлень
- `GET /admin/orders/<id>` - Деталі замовлення
- `POST /admin/orders/<id>/status` - Оновити статус замовлення
- `POST /admin/feedback/delete/<id>` - Видалити відгук
- `GET /admin/logout` - Вихід з адмін-панелі

## 🧪 Тестування

Для запуску тестів (якщо реалізовані):
```bash
python -m pytest tests/
```

## 🔧 Налаштування

### Змінні середовища (.env файл):

```env
FLASK_ENV=production
SECRET_KEY=your-secret-key-here
ADMIN_PASSWORD=your-secure-password
DATABASE_PATH=db.sqlite
HOST=0.0.0.0
PORT=5000
```

## 🛠️ Розробка

### Додавання нових функцій:

1. **Нова модель даних** - додайте в `models.py`
2. **Нові маршрути** - створіть новий blueprint в `routes/`
3. **Нові шаблони** - додайте HTML файли в `templates/`
4. **Реєстрація blueprint** - додайте в `app.py`:
```python
from routes.new_feature import new_bp
app.register_blueprint(new_bp, url_prefix='/feature')
```

## 📊 Статус проєкту

✅ База даних SQLite з повною структурою
✅ CRUD операції для всіх сутностей
✅ Адміністративна панель
✅ Система кошика та замовлень
✅ Система відгуків з рейтингом
✅ Адаптивний дизайн (Bootstrap 5)
✅ Docker підтримка
✅ Production-ready конфігурація
✅ Обробка помилок (404, 500)
✅ Документація

## 🤝 Внесок в проєкт

Вітаємо внески! Якщо ви хочете покращити проєкт:

1. Fork репозиторій
2. Створіть нову гілку (`git checkout -b feature/AmazingFeature`)
3. Commit зміни (`git commit -m 'Add some AmazingFeature'`)
4. Push в гілку (`git push origin feature/AmazingFeature`)
5. Відкрийте Pull Request

## 📄 Ліцензія

Цей проєкт створений в освітніх цілях.

## 👥 Автори

- [@tatshu33-bit](https://github.com/tatshu33-bit)

## 📞 Контакти та підтримка

Якщо у вас виникли питання або проблеми:
- Створіть Issue в GitHub
- Опишіть проблему детально
- Додайте скріншоти (якщо потрібно)

---

**Примітка:** Цей проєкт використовує SQLite для зберігання даних. Для великих production середовищ рекомендується міграція на PostgreSQL або MySQL.
