# Магазин одягу (Flask + SQLite)

Простий приклад інтернет-магазину з:
- SQLite БД
- Збереженням відгуків (feedback)
- CRUD для відгуків
- Таблицями товарів та замовлень
- Зв'язками order → order_items → products
- Проста адмін-панель для керування товарами, перегляду/видалення відгуків та управління статусами замовлень

## 🐳 Запуск з Docker (Рекомендовано)

Найпростіший спосіб запустити проект - використовувати Docker:

```bash
# 1. Клонуйте репозиторій
git clone https://github.com/tatshu33-bit/Store-Clothing.git
cd Store-Clothing

# 2. Створіть файл .env з налаштуваннями
cp .env.example .env
# Відредагуйте .env та встановіть свої паролі

# 3. Запустіть з Docker Compose
docker-compose up -d

# 4. Відкрийте http://localhost:5000
```

**Детальна документація**: Дивіться [DOCKER.md](DOCKER.md) для повної інформації про Docker deployment.

### Docker переваги:
- ✅ Автоматична установка залежностей
- ✅ Ізольоване середовище
- ✅ Персистентність даних БД через volumes
- ✅ Автоматичний health check
- ✅ Легке масштабування та deployment

## 🐍 Локальний запуск (без Docker)

Якщо ви не використовуєте Docker:

1. Встановіть залежності: `pip install -r requirements.txt`
2. Налаштуйте змінні середовища (опційно):
   ```bash
   export SECRET_KEY="your-secret-key"
   export ADMIN_PASSWORD="your-password"
   ```
3. Запустіть: `python app.py`
4. Відкрийте http://127.0.0.1:5000

## 📋 Доступ до адмін-панелі

- URL: http://localhost:5000/admin/login
- Пароль за замовчуванням: `adminpass` (змінити в `.env` або `app.py`)

## 📁 Структура файлів

- `app.py` — стартовий файл
- `models.py` — робота з БД (ініціалізація, CRUD)
- `routes/` — маршрути для магазину, відгуків та адмін-панелі
- `templates/` — HTML-шаблони (Bootstrap)
- `db.sqlite` — створюється автоматично при першому запуску
- `Dockerfile` — Docker image конфігурація
- `docker-compose.yml` — Docker Compose оркестрація
- `DOCKER.md` — детальна Docker документація

## 🔍 Health Check

Додаток має endpoint для моніторингу здоров'я:
- URL: http://localhost:5000/health
- Використовується Docker для автоматичних перевірок
