# 🚀 Керівництво з розгортання ClothesShop

Це керівництво описує процес розгортання веб-додатку ClothesShop на різних платформах.

## 📋 Підготовка до розгортання

### 1. Перевірте конфігурацію

Переконайтеся, що всі налаштування правильні:

- ✅ `SECRET_KEY` встановлений на унікальне значення
- ✅ `ADMIN_PASSWORD` змінений з дефолтного
- ✅ `FLASK_ENV=production` для production середовища
- ✅ База даних створюється автоматично при першому запуску

### 2. Тестування локально

```bash
# Встановіть залежності
pip install -r requirements.txt

# Запустіть з production налаштуваннями
export FLASK_ENV=production
export SECRET_KEY="your-secret-key"
python app.py
```

## 🐳 Розгортання з Docker

### Docker (окремий контейнер)

```bash
# Збудувати образ
docker build -t clothesshop:latest .

# Запустити контейнер
docker run -d \
  -p 5000:5000 \
  -e SECRET_KEY="your-secret-key" \
  -e ADMIN_PASSWORD="your-password" \
  -v ./data:/app/data \
  --name clothesshop \
  clothesshop:latest
```

### Docker Compose

```bash
# Створіть .env файл
cat > .env << EOF
SECRET_KEY=your-super-secret-key-change-this
ADMIN_PASSWORD=your-secure-admin-password
EOF

# Запустіть
docker-compose up -d

# Перегляньте логи
docker-compose logs -f

# Зупиніть
docker-compose down
```

## ☁️ Хмарні платформи

### Render.com

1. **Створіть новий Web Service:**
   - Перейдіть на https://render.com
   - Натисніть "New +" → "Web Service"
   - Підключіть ваш GitHub репозиторій

2. **Налаштуйте сервіс:**
   - **Name:** clothesshop
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `gunicorn --bind 0.0.0.0:$PORT app:app`

3. **Додайте змінні середовища:**
   ```
   SECRET_KEY=your-secret-key-here
   ADMIN_PASSWORD=your-secure-password
   FLASK_ENV=production
   ```

4. **Deploy:** Натисніть "Create Web Service"

### Heroku

```bash
# Увійдіть в Heroku
heroku login

# Створіть додаток
heroku create your-clothesshop-app

# Встановіть змінні середовища
heroku config:set SECRET_KEY="your-secret-key"
heroku config:set ADMIN_PASSWORD="your-password"
heroku config:set FLASK_ENV=production

# Додайте Procfile (якщо ще не існує)
echo "web: gunicorn app:app" > Procfile

# Deploy
git push heroku main

# Відкрийте додаток
heroku open

# Переглянути логи
heroku logs --tail
```

### Railway.app

1. **Створіть новий проєкт:**
   - Перейдіть на https://railway.app
   - Натисніть "New Project"
   - Виберіть "Deploy from GitHub repo"

2. **Railway автоматично:**
   - Виявить Dockerfile
   - Створить production build
   - Призначить домен

3. **Додайте змінні середовища:**
   - Перейдіть в Settings → Variables
   - Додайте:
     - `SECRET_KEY`
     - `ADMIN_PASSWORD`
     - `FLASK_ENV=production`

### Vercel (з адаптером)

Для Vercel потрібен спеціальний файл конфігурації:

```json
// vercel.json
{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
```

### DigitalOcean App Platform

1. **Створіть App:**
   - Перейдіть на https://cloud.digitalocean.com
   - Apps → Create App
   - Підключіть GitHub репозиторій

2. **Налаштування:**
   - **Type:** Web Service
   - **Build Command:** `pip install -r requirements.txt`
   - **Run Command:** `gunicorn --bind 0.0.0.0:8080 app:app`

3. **Environment Variables:**
   ```
   SECRET_KEY=...
   ADMIN_PASSWORD=...
   FLASK_ENV=production
   PORT=8080
   ```

## 🗄️ База даних в Production

### SQLite (поточна конфігурація)

SQLite підходить для малих та середніх навантажень:

**Переваги:**
- ✅ Не потребує окремого сервера
- ✅ Проста конфігурація
- ✅ Достатньо для більшості випадків

**Обмеження:**
- ⚠️ Не підходить для високого навантаження
- ⚠️ Проблеми з concurrent writes

### Міграція на PostgreSQL (рекомендовано для production)

Якщо потрібна більша масштабованість:

1. **Встановіть залежності:**
```bash
pip install psycopg2-binary
```

2. **Оновіть models.py** для підтримки PostgreSQL

3. **Налаштуйте DATABASE_URL:**
```bash
export DATABASE_URL="postgresql://user:password@host:5432/dbname"
```

## 🔒 Безпека в Production

### Обов'язкові налаштування:

1. **Змініть SECRET_KEY:**
```python
import secrets
print(secrets.token_hex(32))
```

2. **Надійний ADMIN_PASSWORD:**
```python
import secrets
print(secrets.token_urlsafe(16))
```

3. **HTTPS:**
- Більшість платформ надають безкоштовний SSL
- Переконайтеся, що HTTPS увімкнено

4. **Firewall та обмеження доступу:**
- Обмежте доступ до адмін-панелі
- Використовуйте rate limiting

### Додаткові рекомендації:

- Використовуйте `.env` файли (не комітьте їх!)
- Налаштуйте логування помилок
- Додайте моніторинг
- Регулярні резервні копії бази даних

## 📊 Моніторинг та логування

### Перегляд логів:

**Docker:**
```bash
docker logs clothesshop
```

**Heroku:**
```bash
heroku logs --tail
```

**Render:**
- Переглядайте в веб-інтерфейсі (Logs tab)

### Рекомендовані сервіси моніторингу:

- **Sentry** - для відстеження помилок
- **New Relic** - для моніторингу продуктивності
- **Datadog** - комплексний моніторинг

## 🔄 CI/CD

### GitHub Actions (приклад)

```yaml
# .github/workflows/deploy.yml
name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy to Render
        run: |
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
```

## 🆘 Усунення проблем

### Додаток не запускається:

1. Перевірте логи
2. Переконайтеся, що всі залежності встановлені
3. Перевірте змінні середовища
4. Перевірте порт (деякі платформи вимагають певний PORT)

### База даних не створюється:

1. Перевірте права доступу до файлової системи
2. Переконайтеся, що шлях до БД правильний
3. Перевірте, чи volume правильно примонтовано (Docker)

### 500 Internal Server Error:

1. Перегляньте логи додатку
2. Перевірте, чи SECRET_KEY встановлений
3. Перевірте конфігурацію бази даних

## 📞 Підтримка

Якщо виникли проблеми з розгортанням:

1. Перевірте документацію платформи
2. Створіть Issue в GitHub
3. Переглянтье логи для деталей помилки

---

**Успішного розгортання! 🚀**
